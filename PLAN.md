# 実装計画:シルエット旅もの・音楽同期映像ジェネレーター

ピアノ/オーケストラ曲に合わせて、シルエットのキャラクターがパララックス(多層スクロール)の風景を旅するゲーム風映像を生成するツール。YouTube 投稿用 MV の第一弾。

## コンセプト

- **入力**: 音源ファイル(wav/mp3)+ トラック設定 JSON(BPM、尺、シーン割り、イベント)
- **出力**: 完成した MP4(1920×1080 / 60fps / 音声合成済み)
- **方式**: リアルタイムプレイや画面録画はせず、フレームを直接描画して ffmpeg に流し込む「オフラインレンダリング」。決定論的(乱数シード固定)なので、何度レンダリングしても同じ映像になり、修正はコードと設定の変更だけで済む

## 技術スタック

| 役割 | 採用技術 | 理由 |
|---|---|---|
| 言語 | TypeScript (Node.js) | Canvas API がそのまま使え、型があるとシーン設定の管理が楽 |
| 描画 | `skia-canvas`(代替: `node-canvas`) | ヘッドレスで 2D Canvas 描画。GPU 支援があり高速 |
| 動画出力 | ffmpeg(rawvideo を stdin パイプ) | 中間 PNG を作らず RGBA バッファを直接 H.264 にエンコード+音声 mux |
| 音解析 | ffmpeg で PCM 抽出 → 自前で RMS 計算 | 音量エンベロープだけ取れれば十分。追加依存なし |

アセットは使わない。空・地形・木・キャラクターすべてコード描画(グラデーション+シルエット)。

## アーキテクチャ

```
tracks/first-piece.json   … 曲ごとの設定(唯一、曲に依存するファイル)
src/
  main.ts                 … CLI: --track --out --preview --from/--to
  pipeline/ffmpeg.ts      … ffmpeg 子プロセス管理、フレームパイプ、音声 mux
  timeline.ts             … 時刻 t → アクティブなシーン/遷移/パレットの解決
  audio/analysis.ts       … PCM 抽出、RMS エンベロープ、ビートグリッド(BPM+offset)
  scene/
    sky.ts                … 縦グラデーションの空、太陽/月、星、流れ星
    terrain.ts            … 1D ノイズの稜線シルエット × 4〜6 層のパララックス
    props.ts              … 木(再帰分岐)、草、遺跡などのシルエット配置
    character.ts          … プロシージャル歩行の旅人+なびくマフラー
    particles.ts          … 雪/雨/蛍/木の葉(プール式、風フィールド)
    postfx.ts             … ビネット、フィルムグレイン、レターボックス
  util/
    noise.ts rng.ts easing.ts color.ts
```

### フレームループ(中核)

```
for frame in 0..totalFrames:
  t = frame / fps
  state = timeline.resolve(t)        // シーン、パレット、遷移係数、音量、ビート
  draw(ctx, state)                   // 奥から順に: 空→遠景→…→キャラ→手前→天候→PostFX
  ffmpeg.stdin.write(canvas.rawRGBA)
```

## 主要コンポーネントの設計

### 1. タイムライン(曲との同期の心臓部)

トラック設定 JSON でシーンとイベントを宣言する:

```jsonc
{
  "audio": "assets/first-piece.wav",
  "bpm": 72, "beatOffsetSec": 0.35, "fps": 60,
  "sections": [
    { "start": 0,   "scene": "predawn", "cameraSpeed": 0.4 },
    { "start": 42,  "scene": "forest",  "cameraSpeed": 1.0, "particles": "fireflies" },
    { "start": 96,  "scene": "snowfield", "particles": "snow" },
    { "start": 148, "scene": "starryHill" },
    { "start": 196, "scene": "sunrise", "cameraSpeed": 0.3 }
  ],
  "events": [
    { "time": 150.2, "type": "shootingStar" },
    { "time": 168.0, "type": "meteorShower" }   // クライマックスに同期
  ]
}
```

- **シーン遷移**: 前後 4〜8 秒でパレット(空・霧・シルエット色)を線形補間しつつクロスフェード。地形は連続生成なので「歩いているうちに景色が変わる」見え方になる
- **RMS エンベロープ → 演出パラメータ**: 音量を風の強さ・パーティクル密度・光量に写像。曲が盛り上がると自然に画面も動く
- **ビートグリッド**: BPM から拍のフレーム番号を算出。蛍の明滅や流れ星の発生を拍に量子化できる

### 2. 空(`sky.ts`)

- 3〜5 ストップの縦グラデーション。シーンごとにパレット定義(未明の紺→茜、森の翠、雪原の鈍色、星空の深藍、朝焼け)
- 星はシード付き乱数で固定配置し、明滅のみ時間関数。流れ星・月・太陽はイベント/シーンで制御

### 3. 地形とパララックス(`terrain.ts` / `props.ts`)

- 各層は 1D バリューノイズの稜線を塗りつぶしたシルエット。奥の層ほど (a) スクロール速度が遅い (b) 空の色に近づけて霞ませる(距離フォグ)
- カメラは等速前進(`cameraSpeed` はセクションで可変)。層のスクロール量 = カメラ位置 × 深度係数
- 木は再帰分岐で生成したシルエット。層ごとにノイズでまばらに配置。すべて決定論的

### 4. 旅人(`character.ts`)

- 画面左 1/3・地面の上に固定(世界の方が流れる)。地形の高さに追従して上下
- 歩行は 2 本足の逆振り子+胴体の上下バウンスのプロシージャルアニメ。歩調はビートに同期させる(拍と足音が合うと気持ちいい)
- マフラー/マントは Verlet 積分のリボン。風パラメータ(RMS 連動)でなびきが変わる — 少ない実装コストで最も「生きて見える」要素

### 5. パーティクル(`particles.ts`)

雪・雨・蛍・木の葉・花びら。オブジェクトプール+風フィールド。密度は RMS とセクション設定で制御。

### 6. PostFX(`postfx.ts`)

ビネット、微量のフィルムグレイン(シード付き)、シネマ用レターボックス(オプション)、全体カラーグレード。

### 7. ゲームらしさの担保(控えめな UI)

要件の「ゲームとしての動き」を補強するため、以下をオプション実装:
- 画面隅の小さな距離カウンタ or 収集物(光の欠片)カウンタ
- 道中に光の欠片を配置し、取得時に淡い SE 的フラッシュ(拍に量子化)
- 冒頭にタイトル画面(ゲーム名ロゴ+"PRESS START")→ 曲の開始と同時にフェードイン

## 開発マイルストーン

| # | 内容 | 完了条件(検証方法) | 目安 |
|---|---|---|---|
| M1 | パイプライン骨格 | 10 秒のグラデーション+拍ごとのフラッシュ動画が音ズレなく出力される(クリック音源で目視/耳検証) | 0.5〜1 日 |
| M2 | 空+地形パララックス | 1 シーンぶんの風景がスクロールする 30 秒動画 | 1 日 |
| M3 | 旅人+カメラ | 歩行・マフラー・地形追従が自然に見える | 1 日 |
| M4 | パーティクル+シーン遷移 | 5 シーンを通しで流し、遷移が滑らか | 1 日 |
| M5 | 実曲マッピング | 第一弾の曲で `tracks/*.json` を作成、通しレンダー | 0.5 日+曲の構成確認 |
| M6 | PostFX+最終画質 | 1080p60 最終書き出し、YouTube アップ検証 | 0.5 日 |

合計 5 人日程度。M1 の音ズレ検証を最初にやるのが重要(ここが崩れていると全部やり直しになるため)。

## プレビュー手段(イテレーション速度の確保)

- `--preview`: 解像度 1/2・フレーム間引きで高速レンダー
- `--from 90 --to 110`: 区間指定レンダーで特定シーンだけ確認
- `--frame 5400 --png`: 単一フレームを PNG 出力(構図確認用)

## リスクと対策

- **レンダリング速度**: 4 分 @60fps = 14,400 フレーム。skia-canvas なら現実的だが、遅ければ解像度を落として ffmpeg 側でアップスケール、またはワーカー並列化
- **音ズレ**: フレーム数を `duration × fps` から厳密に決め、ffmpeg には `-r` を明示。M1 でクリック音源により検証
- **単調さ**: 等速スクロールは飽きやすい。セクションごとの `cameraSpeed` 変化、立ち止まり(休憩イベント)、天候変化で緩急をつける
